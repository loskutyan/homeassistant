air_common:
  sensor:
    - platform: template
      sensors:
        target_temperature:
          friendly_name: 'Target temperature'
          device_class: temperature
          unit_of_measurement: '°C'
          value_template: >
            {% set outside_t = state_attr('weather.yandex_weather', 'temperature') %}
            {{ 22.5 + min(10, max(0, 5 - outside_t)) / 10 }}
